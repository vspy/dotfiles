DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

ln -s $DIR/.vim ~/.vim
ln -s $DIR/.vimrc ~/.vimrc
ln -s $DIR/.git ~/.git
ln -s $DIR/.gitconfig ~/.gitconfig
ln -s $DIR/.settings.bash ~/.settings.bash
